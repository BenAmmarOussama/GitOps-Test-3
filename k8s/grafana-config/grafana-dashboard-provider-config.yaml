apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-provider-config
  namespace: monitoring
data:
  dashboard.yaml: |
    # # config file version
    apiVersion: 1

    providers:
    - name: 'dashboards'
      type: file
      options:
        path: /etc/grafana/dashboards